const car = {
    brand: "bluejgiul",
    model: "da",
    color: "red"
  };
  
  console.log("Brand: " + car.brand);
  console.log("Model: " + car.model);
  console.log("Color: " + car.color);
  
  const card = ` ${car.brand} brand, r ${car.model} ,   ${car.color} .`;
  console.log(card);
  